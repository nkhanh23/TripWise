# Authorization

The current conversation explicitly authorizes deployment of T004 migrations to the linked TripWise Supabase project, creation and mutation of bounded disposable remote identities/data, authenticated runtime evidence, and cleanup of disposable evidence data.

The linked target was independently rechecked before remote work: TripWise, project ref `bvblyrzbkyhcreimuumu`, region `ap-northeast-1`, status `ACTIVE_HEALTHY`.

No credential, access token, refresh token, JWT, service-role key, or database password is stored here.
